{
  "name": "kdp",
  "private": true,
  "scripts": {
    "dev": "npm --prefix next-site run dev",
    "build": "npm --prefix next-site run build",
    "start": "npm --prefix next-site run start",
    "lint": "npm --prefix next-site run lint"
  }
}
